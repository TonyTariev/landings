# landings

Output repository for landings auto-generated by the `create-landing` skill (lives in the `Создатель лендингов` Claude Code workspace).

Each landing lives in its own subfolder:

```
landings/
├── <slug>/
│   └── index.html
└── README.md
```

Live URL: `https://tonytariev.github.io/landings/<slug>/`.

## How it works

A custom Claude Code skill `create-landing` runs a short content interview, generates a single-file `index.html` (HTML + Tailwind CDN), commits to this repo, pushes to `main`. GitHub Pages serves it within ~30–60 seconds.

This repo is the *output*. The generator (skill code, project knowledge, planning) lives in a separate project — `Создатель лендингов`.

## Manual edits

Edits via the `create-landing` skill are preferred. Manual edits to a landing's `index.html` work too — push to `main`, deploy is automatic.
